<div class="container">
  <%= simple_form_for [episode, comment] do |f| %>
    <%= f.input :title, label: false, placeholder:"Title" %>
    <%= f.input :content, label:false, placeholder:"Comment" %>
    <%= f.button :submit, "Post", class: "btn btn-light button-podfast" %>
  <% end %>
</div>
