<div class="container">
  <%= simple_form_for @comment do |f| %>
    <%= f.input :title %>
    <%= f.input :content %>
    <%= f.button :submit, "Edit", class: "btn btn-light button-podfast" %>
  <% end %>
</div>
