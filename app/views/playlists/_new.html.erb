

  <%= simple_form_for @playlist, url: user_playlists_path(current_user), method: :post do |f| %>
    <%= f.input :episode, :as => :hidden, :input_html => { :value => @episode.id } %>
    <%= f.text_field :name, class: "create-new-playlist-form"%>
    <%= f.submit %>
  <% end %>


